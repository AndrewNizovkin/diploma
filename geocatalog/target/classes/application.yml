server:
  error:
    include-message: ALWAYS
spring:
  application:
    name: geocatalog
  datasource:
    url: jdbc:postgresql://db:5432/basepointsdb
    username: andrew
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
